on:
  pull_request:
    types: [opened, synchronized]
name: "Template for PRs"
jobs:
  testbox:
    runs-on: ubuntu-latest
    env:
      REF_TAG: ${{ github.ref }}
    steps:
      - name: Print Ref tag
        run: echo "$REF_TAG"
